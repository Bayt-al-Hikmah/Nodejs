// Converting between Types

// Convert to Intenger
console.log(Number("45"));           // result 45   | it return number or NaN if it fail
console.log(parseInt("42.5"));       // result 42   |
console.log(parseFloat("42.5"))      // result 42.5 |

// Convert to String
let message = (100).toString();   // result "100"
let status = String(true);        // result "true"
